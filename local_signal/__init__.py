"""Local Guppy RSI signal generation (replaces the TradingView webhook source).

See `.context/plans/TRK-001.md`. The pure, dependency-free signal logic lives in
`guppy.py` so it is identically runnable in the backtest and live (parity).
"""
